# 라운드 3 — 전체 백로그 소진 구현 계획

> **실행자: Codex** (이 문서 단독으로 실행 가능). 날짜: 2026-06-12
> 근거: `docs/신규자료-분석-20260611.md`(갭분석 — 슬라이드 번호 출처), `_extracted/통합본_완전판.md`, `_extracted/개정세법특강_완전판.md`, 라운드 2 계획 `docs/plans/2026-06-12-라운드2-신뢰성-후속콘텐츠-계획.md`

**Goal:** 남은 백로그 전부 소진 — ① 레거시 fact 83건 원문 검증, ② 미확정 fact 65건 정리, ③ 계산기 2건(salary-net-pay 보강 + 연말정산 시뮬레이터), ④ 콘텐츠 갭 전부(4대보험 총괄·유권해석 코너·외국인 19%·감면·이자 13유형·챕터 보강·치트시트 잔여).

**전제(완료분 — 다시 만들지 말 것):** 라운드 1(PR #1)·라운드 2(PR #2) 머지 완료. 챕터 17개(`content/chapters/*.mdx` — ch1~ch10, daily-worker, employee-local-tax, nonresident, interest-dividend, glossary, index, cheatsheet), 계산기 12종, 조세조약 34개국 표(`content/tax-rules/2026/treaty-rates.json` + nonresident.mdx §11), penalty 2026.7.0 실계산, 고위험 fact 35건 검증 완료. 현재 facts 237건(확정 172 / 확인필요 39 / 강의기반 26).

## 원칙 (라운드 2와 동일 — 위반 시 반려)

1. 법령 원문 미확인 수치는 '확인필요'/'강의기반' 유지 — '확정' 남발 금지. 승격 시 `primarySourceVerified: true` + `history` 기록.
2. TDD — 계산 로직은 실패 테스트 먼저. 기존 vitest 컨벤션(순수 계산 → 룰↔lib 교차검증 → UI 렌더).
3. 디자인·컴포넌트 기존 패턴(`<Tbl>`, `<F id>`, `<Box>`, `RuleBasis`) 유지.
4. law.go.kr OPEN API OC 키는 비밀 — 커밋 파일 금지, env `LAW_OC`. 사용자 노출 링크는 공개형 `https://www.law.go.kr/법령/...`.
5. 검증 방법(라운드 2와 동일): `lawSearch.do`로 MST 획득 → `lawService.do`로 조문·부칙 원문 대조 → fact 갱신. 유권해석은 국세법령정보시스템(taxlaw.nts.go.kr) 문서번호 검색.

**신규 fact ID 대역(기존 prefix와 무충돌 확인됨):** 유권해석 `f_qa00xx` / 감면 `f_rl00xx` / ch6 보강 `f_r600xx` / ch8 보강 `f_r800xx` / ch9 보강 `f_r900xx` / 연말정산 시뮬 `f_ye00xx` / 이자·배당 보강 기존 `f_id0013`부터 이어쓰기 / 치트시트 기존 `f_cs0005`부터 / 4대보험 기존 `f_si0009`부터. 추가 전 해당 prefix를 `content/facts.json`에서 grep으로 재확인.

---

## Phase 1 — 신뢰성 (최우선)

### Task 1: 레거시 fact 83건 — primarySourceVerified 일괄 검증
**Files:** `content/facts.json`, `docs/REVIEW-QUEUE.md`(재생성)

대상: `verifyStatus: '확정'` ∧ `primarySourceVerified: false` 83건 (ch4 13·ch5 10·ch9 10·ch1 9·ch2 9·ch7 9·ch8 8·ch6 7·ch10 3·ch3 2·interest-dividend 2·nonresident 1).
- [ ] 각 fact의 `lawRef` 조문을 API 원문과 대조 → 일치 시 `primarySourceVerified: true` + history `{date:'2026-06-12', author:'codex', note:'원문 확인(...)'}`. 불일치 시 claim 교정 + `previousValue` 기재.
- [ ] 조문이 아닌 출처(예규·실무관행·교재) 기반이라 원문 검증 자체가 불가한 항목은 psv를 올리지 말고 `scopeLimitations`에 사유 1줄 기록 — 억지 승격 금지.
- [ ] 챕터 단위 분할 커밋(예: `fix(facts): ch4 레거시 13건 원문 검증`). 전 건 처리 후 `npm run gen:queue`.

**DoD:** 83건 전부 psv=true 또는 사유 기록. `npm run check:freshness` 통과.

### Task 2: 미확정 fact 65건 정리 (확인필요 39 + 강의기반 26)
**Files:** `content/facts.json`

- [ ] '확인필요' 39건: Task 1과 동일 절차로 원문 확인 → '확정' 승격 시도. 고시·시행규칙 별표(간이세액표 등)는 국세청/공단 공지로 확인 후 sourceUrl 기록.
- [ ] '강의기반' 26건 중 **정당 잔존**(경험칙·문서번호 미확인: `f_lt0018`, `f_u26004` 등 scopeLimitations에 이미 사유 있는 것)은 건드리지 말 것. 나머지는 원문 탐색 1회 시도 후 승격 또는 사유 기록.
- [ ] `f_tt0001~2` + `content/tax-rules/2026/treaty-rates.json` 34개국 세율: 국세법령정보시스템 조세조약 본문 또는 기획재정부 조세조약 자료로 **전수 대조**. 불일치 국가는 JSON·mdx 표(`content/chapters/nonresident.mdx` §11) 동시 교정 + note 기록. 전수 확인 완료 시 파일 레벨 `verifyStatus`를 '확정'으로.

**DoD:** 미확정 잔존 건수가 "사유 기록된 정당 잔존"만 남음.

### Task 3: 조세조약 mdx↔JSON drift 방지 테스트
**Files:** `test/treaty-rates.test.ts`
- [ ] nonresident.mdx §11 `<Tbl>`의 34개 행을 정규식 파싱해 JSON `rates`와 국가명·세율 문자열 일치 검증하는 테스트 추가(예: JSON `[0.05,0.15]` ↔ mdx `5/15%` 변환 함수 포함). 실패 확인 후—이미 일치하므로 바로 통과해야 정상—커밋.

---

## Phase 2 — 계산기

### Task 4: `salary-net-pay` 4대보험 요율 자동계산 보강
**Files:** Modify `components/calculators/SalaryNetPayCalculator.tsx`, `lib/salary-net-pay/`(기존 lib 경로 grep으로 확인), `content/tax-rules/2026/social-insurance-2026.json`(필요 시 룰 버전 추가) / Test `test/salary-net-pay.test.tsx`

현재 4대보험을 사용자 직접 입력. 검증 완료된 `social-insurance-2026.json` 요율(라운드 2에서 고시 확인)로 자동계산 모드 추가.
- [ ] 실패 테스트 먼저: 국민연금 상·하한 클램프('26.6 이전/'26.7 이후 상한 분기 — 룰 JSON 값 사용), 건보+장기요양, 고용보험 케이스. `reverse-net-pay`의 `lib/reverse-net-pay/calc.ts`에 동일 공제 로직 이미 존재 — **중복 구현 금지, 공통 모듈로 추출**(예: `lib/social-insurance/deductions.ts`)해 양쪽에서 사용. 기존 reverse-net-pay 테스트 무손상 확인.
- [ ] 한백택스 포털 코드의 함정 회피(갭분석 §3-3): (a) 포털의 국민연금 상한 타임라인은 오류 — 우리 룰 JSON 값 사용, (c) `new Date()` 사용 금지 — 귀속연도 select 입력, (d) 비과세는 기존 `non-taxable-cap` 룰의 한도 클램핑 재사용(식대 입력 50만 → 한도 초과분 과세 처리), (e) 간이세액표 아닌 연 환산 근사임을 UI 면책 명시.
- [ ] 수동 입력 모드는 유지(자동/수동 토글). 커밋.

### Task 5: 연말정산 결과 시뮬레이터 (신규 계산기)
**Files:** Create `app/calculators/year-end-simulator/page.tsx`, `app/tools/year-end-simulator/page.tsx`(1줄 re-export), `components/calculators/YearEndSimulator.tsx`, `lib/year-end/simulate.ts` / Modify `content/tax-rules/2026/year-end-settlement.json`(룰 append: `year_end_result_estimate` v2026.1.0), `lib/tool-priorities.ts`, `content/facts.json`(`f_ye00xx`) / Test `test/year-end-simulator.test.tsx`

- [ ] 룰 정의 먼저(SDD): inputs = 총급여, 기납부세액, 인적공제 인원(본인 외), 자녀 수(세액공제 — 2026 개정값은 기존 `f_u26001`·ch9 fact 참조), 연금보험료, 직접입력 공제 합계. formula = 근로소득공제(구간) → 과세표준 → 기본세율(6~45% 누진) → 산출세액 → 근로소득세액공제(한도) + 자녀세액공제 → 결정세액 → 결정세액−기납부 = 환급(−)/추가납부(+). examples 3건(환급/추납/0원 경계) 포함.
- [ ] 실패 테스트 → 구현 → 룰↔lib 교차검증 테스트 → UI(지방소득세 10% 병기, `RuleBasis` 부착).
- [ ] 면책 명시: 신용카드·의료비 등 공제율 자동계산 없음(공제액 직접입력 방식 한계), 간이 추정용. 한백택스 포털의 `childAgeMin()` 죽은 코드·죽은 출력 id는 이식하지 말 것.
- [ ] `lib/tool-priorities.ts` 등록(tier P1, status available) + 커밋.

---

## Phase 3 — 콘텐츠

> 공통: 수치 주장 = fact 등록 + `<F id>` 래핑. 슬라이드 전사값 임의 보정 금지. 원문 확인 안 되면 '강의기반'. 각 태스크 끝에 `npm run typecheck && npm test` + 커밋.

### Task 6: 신규 챕터 `social-insurance` — 2026 4대보험·최저임금 총괄 (특강 S99~102)
**Files:** Create `content/chapters/social-insurance.mdx` / Modify `lib/chapter-meta.ts`(CHAPTERS에 `{slug:'social-insurance', title:'2026 4대보험·최저임금 총괄', cat:'payroll'}`), `content/facts.json`(`f_si0009`부터)
- [ ] 요율 총괄표(근로자/사용자 부담 분리): 국민연금 9.5%('33년 13%까지 매년 0.5%p 인상 로드맵), '26.7~ 기준소득월액 상한 637만/하한 40만, 건강보험 7.19%, 장기요양 13.14%(건보료 대비), 고용보험. 수치는 **라운드 2에서 확정된 `f_si0001~0008` + `social-insurance-2026.json` 값과 일치**시킬 것 — 새로 전사하지 말 것.
- [ ] 최저임금 10,320원/월 환산(209h) — 신규 fact, 고용노동부 고시로 확인.
- [ ] salary-net-pay·reverse-net-pay 계산기 링크 배치.

### Task 7: 신규 챕터 `rulings` — 유권해석·사례 코너 (통합본 S330~332·377·391 + 특강 S285~297)
**Files:** Create `content/chapters/rulings.mdx` / Modify `lib/chapter-meta.ts`(cat `income` 또는 `appendix` — APPENDICES 쪽이면 그 배열에), `content/facts.json`(`f_qa00xx`), `content/sources.json`(필요 시 출처 추가)
- [ ] 소득구분 4사례: 강사료(소득세과-0973), 특허권(원천-457), 사외이사(소득-662), 서면1팀-464 — 각각 사실관계→쟁점→결론 카드(`.case` 패턴 상응 컴포넌트).
- [ ] 산학협력 연구용역 6패턴(S377·391), 2026.1~3월 최신 유권해석 6건(특강 S285~297 — 특히 통상임금 판결 소급분: 서면-2026-원천-4165, 귀속=근로제공일, 판결 다음달 말까지 재연말정산).
- [ ] 문서번호는 국세법령정보시스템에서 실존 확인 → sourceType `INTERPRETATION`, 확인된 것만 '확정'. 미확인은 '강의기반' + scopeLimitations.

### Task 8: 외국인 19% 단일세율 + 중소기업취업자 감면
**Files:** Modify `content/chapters/ch8.mdx`(외국인 — 기존 손익분기 fact 1건 존재: '강의기반' 잔존 항목과 중복 금지, 먼저 `chapter:"ch8"` grep), `content/chapters/ch6.mdx`(감면), `content/facts.json`(`f_r800xx`/`f_rl00xx`)
- [ ] ch8: 외국인 *거주자* 단일세율 섹션(통합본 S154~155) — 조특법 §18조의2: 총급여×19%, 비과세·공제·감면·세액공제 전부 배제, 적용기한·국내 근무 시작 20년 이내 요건 원문 확인. 일반 연말정산과 유불리 비교는 "개인별 상이" 서술 — **손익분기 단정 금지**(기존 fact의 scopeLimitations 결론 유지).
- [ ] ch6: 중소기업취업자 소득세 감면 섹션(통합본 §1-④ — 갭분석 35행): 청년 90%(그 외 70%)·연 200만 한도·청년 5년(그 외 3년), 조특법 §30 원문으로 세율·기한 확인. 성과공유 경영성과급 50%(조특법 §19), 핵심인력 성과보상기금, 우수인력 국내복귀 50%·10년은 한 단락 요약 수준.

### Task 9: 기존 챕터 보강 4건 (갭분석 §2 잔여)
**Files:** Modify `content/chapters/interest-dividend.mdx`, `ch9.mdx`, `ch6.mdx`, `ch8.mdx`, `nonresident.mdx`, `content/facts.json`
- [ ] **interest-dividend**(`f_id0013`부터): 이자소득 13유형 세율 표(S480~482 — 직장공제회 초과반환금 기본세율, 비실명 45%·금융실명법 위반 90%, 공모부동산집합투자 9%), 귀속시기 9유형 표(S483), IMA 배당 분류 신설(특강 S39), 외국법인 장외파생 배당 범위 확대(특강 S90).
- [ ] **ch9**(`f_r900xx`): 퇴직소득 근속연수공제 2023 확대 전후 비교표(30/50/80/120만→100/200/250/300만, S288·435), 환산급여공제 5구간(S290·436), 노란우산공제 퇴직소득 인정(S418).
- [ ] **ch6/ch8**(`f_r600xx`/`f_r800xx`): 식대 케이스 5종 FAQ(S79), 자가운전보조금 부부공동명의 인정·리스 불가(S81), 보험료 계약자/피보험자/수익자 매트릭스(S89), 복지포인트 판례 대립(서울행법 과세 vs 대전고법 비과세 — `risk: 'high'` + 양설 병기 필수, S93).
- [ ] **nonresident**: 비거주연예인 20% 강제 특례(소법 §156의5, S491) — 기존 mdx에 없으면 추가(먼저 grep '연예인').

### Task 10: 치트시트 잔여 표 3종
**Files:** Modify `content/chapters/cheatsheet.mdx`, `content/facts.json`(`f_cs0005`부터)
- [ ] 간이지급명세서·지급명세서 제출시기×가산세 통합표(S28·S33 — 라운드 1 f_u26 간이지급명세서 반기 제출 fact와 모순 없게), 근로소득 수입시기 11유형 표(S63~66), 기본세율 간편법 표(S263).

---

## Phase 4 — 마무리

### Task 11: 운영 갱신 + PR
- [ ] `npm run gen:queue`(REVIEW-QUEUE 재생성), `npm run check:links -- --write`(신규 출처 링크 스냅샷).
- [ ] 최종 게이트: `npm run qa` + `npm run build` 전체 통과. 신규 라우트 수동 확인: `/ch/social-insurance`, `/ch/rulings`, `/tools/year-end-simulator`, `/tools/salary-net-pay`.
- [ ] 분할 커밋 유지(Phase·챕터 단위), 브랜치 `codex/round3-backlog` → PR 생성(base main). PR 본문에 fact 통계 변화(확정/psv 건수 before→after) 기재.
- [ ] 본 계획 문서(`docs/plans/2026-06-12-라운드3-전체-백로그-소진-계획.md`, 현재 untracked)도 첫 커밋에 포함할 것.

## 제외 (영구 또는 별도 결정)
- 한백택스 양도세·증여세·종소세·환차손익·건물기준시가 (범위 밖/결함)
- 통합본 S255~258 "전산분석 회피" 팁 (게재 부적절)
- 가상자산 기타소득 '27.1.1 — watchlist로 관리 중(작업 불요)
